# -*- encoding: utf-8 -*-

Gem::Specification.new do |s|
  s.name = "dealer"
  s.version = "0.0.1"

  s.required_rubygems_version = Gem::Requirement.new(">= 0") if s.respond_to? :required_rubygems_version=
  s.authors = ["Ammar Nasikwala"]
  s.date = "2012-09-27"
  s.description = "It is gem for generating deals in the bridge game"
  s.email = "ammar_nasikwala@persistent.co.in"
  s.extra_rdoc_files = ["README", "LICENSE"]
  s.files = ["README", "LICENSE"]
  s.require_paths = ["lib"]
  s.rubygems_version = "1.8.23"
  s.summary = "It is gem for generating deals in the bridge game."

  if s.respond_to? :specification_version then
    s.specification_version = 3

    if Gem::Version.new(Gem::VERSION) >= Gem::Version.new('1.2.0') then
    else
    end
  else
  end
end
