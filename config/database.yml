# MySQL.  Versions 4.1 and 5.0 are recommended.
# 
# Install the MYSQL driver
#   gem install mysql
#
# Ensure the MySQL gem is defined in your Gemfile
#   gem 'mysql2'
#
# And be sure to use new-style password hashing:
#   http://dev.mysql.com/doc/refman/5.0/en/old-client.html
development:
  #adapter: mysql
  adapter: postgresql
  encoding: utf8
  reconnect: false
  database: BidPal_development
  #database: BidPal_development_new
  pool: 5
  username: postgres	#root
  password: ps7153!@#	#root
  host: localhost

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  #adapter: mysql
  adapter: postgresql
  encoding: utf8
  reconnect: false
  database: BidPal_test
  #database: BidPal_test_new
  pool: 5
  username: postgres
  password: ps7153!@#
  host: localhost

production:
  #adapter: mysql
  adapter: postgresql
  encoding: utf8
  reconnect: false
  database: BidPal_production
  #database: BidPal_production_new
  pool: 5
  username: postgres
  password: ps7153!@#
  host: localhost
